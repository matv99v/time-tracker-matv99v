import React from 'react';
import Glyphicon from 'react-bootstrap/lib/Glyphicon';

export default props => (
    <div style={{ height: 120 }}>
        <Glyphicon glyph="user"style={{ 'fontSize': '50px' }}/>
    </div>
);
